@model IEnumerable<RGBViagens.Models.Destinos>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome_lugar)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Preco)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Preco_Promoção)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome_lugar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Preco)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Preco_Promoção)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Destinos">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Destinos">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Destinos">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
